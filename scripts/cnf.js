export function convertToCNF(formula) {
    // Aquí deberías implementar la lógica de conversión a CNF.
    // Este es un ejemplo básico y no una implementación completa.
    // Normalmente se usarían librerías especializadas para esto.
  
    // Ejemplo: Reemplazo simple (este no es un verdadero algoritmo de CNF)
    return formula.replace(/\|\|/g, '∧').replace(/&&/g, '∨');
  }
  